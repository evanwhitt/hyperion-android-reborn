## What does this change?

<!-- Describe what you changed and why. Keep it focused - one feature/fix per PR. -->

## Related issue

<!-- Link the issue(s) this closes, e.g. "Fixes #12" -->

## Testing

- [ ] Built successfully (`./gradlew :app:assembleDebug`)
- [ ] Unit tests pass (`./gradlew :app:testDebugUnitTest`)
- [ ] Tested on a device / emulator (which one?)

## Checklist

- [ ] Code follows the existing style (no needless comments, consistent naming)
- [ ] Added a unit test if this fixes something testable
- [ ] Did not bump the version or touch the changelog (releases are handled separately)
